{
	"name": "minter-tests",
	"version": "1.0.0",
	"description": "",
	"main": "./src/app.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"build": "rimraf dist/ && babel ./ --out-dir dist/ --ignore ./node_modules,./.babelrc,./package.json,./npm-debug.log --copy-files",
		"start": "npm run build && set NODE_ENV='development' && node dist/app.js"
	},
	"author": "",
	"license": "ISC",
	"dependencies": {
		"lodash.has": "^4.5.2",
		"minimist": "^1.2.0",
		"minter-js-sdk": "^0.3.0",
		"minterjs-wallet": "^2.1.1",
		"nconf": "^0.10.0",
		"winston": "^3.1.0"
	},
	"devDependencies": {
		"babel-cli": "^6.26.0",
		"babel-preset-env": "^1.7.0",
		"babel-preset-es2015": "^6.24.1",
		"esm": "^3.0.84",
		"rimraf": "^2.6.2"
	}
}
